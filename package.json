{
  "name": "@curio/contracts",
  "version": "0.1.0",
  "description": "Curio shared API/DB contracts — generated TS types + a Swift package, from one Zod/schema source. Consumed by pokemon-tool (web) and curio-capture-ios.",
  "license": "UNLICENSED",
  "private": false,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "Sources",
    "Package.swift"
  ],
  "scripts": {
    "gen:db": "tsx scripts/gen-db-types.ts",
    "build": "tsc -p tsconfig.build.json && tsx scripts/gen-swift-db.ts && tsx scripts/gen-swift-api.ts",
    "check": "tsx scripts/check-drift.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "tsx": "^4.16.2",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5",
    "zod-to-json-schema": "^3.23.2"
  }
}
